# Morgana v0.2.6 Release

**Date:** 2026-04-28  
**Previous HEAD:** f22a3ce  
**Branch:** master

---

## Changes

### Version bump to 0.2.6
- `server/config.py`: `version = "0.2.6"`
- `installer/MorganaServerSetup.iss`: `#define MyAppVersion "0.2.6"`

### Build script fixes
- `scripts/build-installer.ps1`: Fixed regex to match `version: str = "..."` (type-annotated class attribute) — was reading `0.0.0` due to missing `: str` in pattern
- `scripts/build-installer.ps1`: Fixed em dash encoding issue (`—` -> `-`) causing PowerShell parser error
- `build/build-server.py`: VERSION now read dynamically from `server/config.py` instead of hardcoded `"0.1.0"`

### Merlino CDN
- `Merlino/docs/morgana/Morgana-Server-Setup.exe`: updated to 0.2.6 build
- `Merlino/docs/morgana/morgana-server.exe`: updated to 0.2.6 build
- `Merlino/docs/morgana/version.json`: `"version": "0.2.6"`, release notes updated

### New features in this release (since 0.2.5)
- Uninstall Agent modal in Web UI (platform-specific commands for Windows/Linux)
- In-app auto-update system (banner when CDN version > installed)
- CDN migrated from Camelot GitHub raw to `https://merlino.x3m.ai/morgana/` (Cloudflare Pages)
- All README download URLs updated to merlino.x3m.ai
- Linux manual install documentation added
- Windows Defender exclusion note for atomics/ submodule added
- `build-installer.ps1` auto-publishes to Merlino CDN after every build
